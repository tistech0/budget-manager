# Production Environment Variables
# Using relative path for Docker deployments - nginx proxies /api to backend container
# For custom domains, nginx handles the routing automatically
VITE_API_BASE_URL=/api
VITE_API_TIMEOUT=15000
